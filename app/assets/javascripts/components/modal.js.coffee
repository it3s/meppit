#= require jquery.modal

App.components.modal = (container) ->
  {
    container: container,

    init: ->
      @start()

    start: ->
      # @container.modal()
      console.log('components: started modal');
  }
