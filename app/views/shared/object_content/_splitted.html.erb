<div class="side-pane">
  <%= render local_assigns[:side_pane] || "#{parent_crl_name}/side_pane", f: local_assigns[:f], name => object %>
</div>

<div class="central-pane">
  <%= render local_assigns[:header] || "#{parent_crl_name}/header",  f: local_assigns[:f], name => object %>
  <%= render('shared/location',  f: local_assigns[:f], object: object) unless local_assigns[:with_location?] == false %>

  <%= render local_assigns[:content] || "#{parent_crl_name}/content",  f: local_assigns[:f], name => object, contributions: @contributions %>
</div>
