<ul class="toolbar" role="toolbar">
  <% tools_list(object, local_assigns[:only] || :all).each do |tool| %>
    <li class="<%= 'active' if request.path == tool[:url] %>" title="<%= tool[:title] %>">
      <a href="<%= tool[:url] %>" role="button">
        <%= icon tool[:icon], '', :class => "fa-lg fa-fw" %>
      </a>
    </li>
  <% end %>
</ul>
