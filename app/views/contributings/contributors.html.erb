<% if request.xhr? %>

  <%= render 'contributings/contributors' %>

<% else %>

  <%= render 'shared/object',
    presenter: ObjectPresenter.new(object: @contributable, ctx: self,
      show_location: false, content: 'contributings/contributors')
  %>

<% end %>
