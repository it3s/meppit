<%= link_to_modal icon("code"), "#embed-modal", html: {class: "embed button", title: t('embed.link_title')} %>

<div id="embed-modal" class="embed-modal modal" data-components="embedder" data-embedder-options="<%= {baseUrl: embed_v1_map_path(only_path: false)}.to_json %>">
  <header><h2><%= t 'embed.title' %></h2></header>

  <section>
    <p class="explanation"><%= t 'embed.explanation' %></p>

    <textarea id="embed-code" class="embed-code"></textarea>

    <div class="embed-options options">
    </div>
    <!-- <div class="help">
      <%= link_to_modal t('embed.help'), help_embed_v1_maps_path, remote: true %>
    </div> -->
  </section>
</div>
