<li class="list-item small notification <%= notification.status %>">
  <div class="wrapper">
    <a href="<%= notification.url %>" title="<%= notification.name %>">
      <div class="item-avatar">
        <%= notification.avatar %>
      </div>
    </a>
    <div class="item">
      <div class="notification-event">
        <%= link_to notification.author.name, user_path(notification.author) %>
        <span class="event-type"><%= notification.event_type %></span>
        <%= link_to notification.name, notification.url %>
        <time class="notification-time" datetime="<%= notification.time.to_datetime.rfc3339 %>">
          <%= notification.time_ago %>
        </time>
      </div>
      <div class="notification-changes">
        <%= notification.changes %>
      </div>
    </div>
  </div>
</li>
