<% if request.xhr? %>

  <%= render 'followings/following' %>

<% else %>

  <%= render 'shared/object',
    presenter: ObjectPresenter.new(object: @followable, ctx: self,
      show_location: false, content: 'followings/following')
  %>

<% end %>
